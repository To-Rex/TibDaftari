/**
 * 'admin' namespace. Keys used as t('admin.someKey'). uz is the base; ru/en fall back to uz.
 * Owned by the admin module — add keys here, keep alphabetical-ish grouping by page.
 */
export const adminLocale = {
  uz: {} as Record<string, unknown>,
  ru: {} as Record<string, unknown>,
  en: {} as Record<string, unknown>,
}
