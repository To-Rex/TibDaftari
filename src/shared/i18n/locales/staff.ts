/**
 * 'staff' namespace. Keys used as t('staff.someKey'). uz is the base; ru/en fall back to uz.
 * Owned by the staff module — add keys here, keep alphabetical-ish grouping by page.
 */
export const staffLocale = {
  uz: {} as Record<string, unknown>,
  ru: {} as Record<string, unknown>,
  en: {} as Record<string, unknown>,
}
