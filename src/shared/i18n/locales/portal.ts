/**
 * 'portal' namespace. Keys used as t('portal.someKey'). uz is the base; ru/en fall back to uz.
 * Owned by the portal module — add keys here, keep alphabetical-ish grouping by page.
 */
export const portalLocale = {
  uz: {} as Record<string, unknown>,
  ru: {} as Record<string, unknown>,
  en: {} as Record<string, unknown>,
}
