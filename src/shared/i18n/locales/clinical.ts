/**
 * 'clinical' namespace: lab, confirm, reports, messages pages. t('clinical.lab.xxx').
 * uz is base; ru/en fall back to uz.
 */
export const clinicalLocale = {
  uz: {} as Record<string, unknown>,
  ru: {} as Record<string, unknown>,
  en: {} as Record<string, unknown>,
}
