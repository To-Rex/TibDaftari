{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "22"
  },
  "steps": {
    "install": {
      "commands": ["npm ci --no-audit --no-fund"]
    },
    "build": {
      "commands": ["npm run build"]
    }
  }
}
